# Streamlit 앱 설치 및 실행 가이드

## 🔍 사전 점검 사항

### 1. 필수 파일 확인
다음 파일들이 상위 디렉토리에 있어야 합니다:
- ✅ `auto_trading_system_gui.py` (필수)
- ✅ `api.json` (자동매매 사용 시 필수)

### 2. Python 버전 확인
```bash
python --version
```
- Python 3.8 이상 필요

## 📦 설치 방법

### 방법 1: 가상환경 사용 (권장)

```bash
# 1. 프로젝트 루트에서 가상환경 생성
python -m venv venv

# 2. 가상환경 활성화
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate

# 3. streamlit_app 폴더로 이동
cd streamlit_app

# 4. 패키지 설치
pip install -r requirements.txt
```

### 방법 2: 전역 설치

```bash
# streamlit_app 폴더로 이동
cd streamlit_app

# 패키지 설치
pip install streamlit pandas pyupbit requests rich
```

## 🚀 실행 방법

```bash
# streamlit_app 폴더에서 실행
streamlit run app.py
```

브라우저가 자동으로 열립니다: `http://localhost:8501`

## ⚠️ 문제 해결

### 1. ModuleNotFoundError: 'pyupbit' 모듈을 찾을 수 없습니다

**원인**: 패키지가 설치되지 않음

**해결**:
```bash
pip install pyupbit
# 또는
pip install -r requirements.txt
```

### 2. Import 오류: auto_trading_system_gui

**원인**: 상위 디렉토리에 `auto_trading_system_gui.py` 파일이 없음

**해결**: 
- 상위 디렉토리(`업비트3분봉` 폴더)에 `auto_trading_system_gui.py` 파일이 있는지 확인
- 파일이 있다면 경로가 올바른지 확인

### 3. tkinter 관련 오류

**원인**: Streamlit 환경에서 tkinter가 필요 없지만 import 시도

**해결**: `utils.py`에서 tkinter를 mock 처리하도록 수정됨 (이미 반영됨)

### 4. API 키 오류

**원인**: `api.json` 파일이 없거나 잘못된 형식

**해결**:
- 상위 디렉토리에 `api.json` 파일 생성
- 형식 확인:
```json
{
    "access_key": "YOUR_ACCESS_KEY",
    "secret_key": "YOUR_SECRET_KEY"
}
```

### 5. 포트 충돌

**원인**: 포트 8501이 이미 사용 중

**해결**:
```bash
streamlit run app.py --server.port 8502
```

## 📝 체크리스트

실행 전 확인:
- [ ] Python 3.8 이상 설치됨
- [ ] 상위 디렉토리에 `auto_trading_system_gui.py` 파일 존재
- [ ] `streamlit_app/requirements.txt`의 패키지 설치 완료
- [ ] 자동매매 사용 시 `api.json` 파일 준비됨
- [ ] `streamlit_app` 폴더에서 실행 중

## 🎯 빠른 시작

```bash
# 1. streamlit_app 폴더로 이동
cd streamlit_app

# 2. 패키지 설치 (처음 한 번만)
pip install -r requirements.txt

# 3. 실행
streamlit run app.py
```

## 📚 추가 정보

- 상세한 사용 방법: `README.md` 참고
- 필터링 조건: `../필터링_조건_전체_정리.md` 참고
