# Railway IP 주소 확인 및 업비트 API 등록 가이드

## 🔍 Railway IP 주소 확인 방법

Railway는 **동적 IP**를 사용하므로 고정 IP 주소가 없습니다. 하지만 출발(Outbound) IP 주소를 확인할 수 있습니다.

### 방법 1: Railway 앱에서 IP 확인 (권장)

Railway에 배포된 앱에서 다음 코드를 실행하여 IP를 확인합니다:

```python
import requests

# 외부 서비스를 통해 IP 확인
response = requests.get('https://api.ipify.org?format=json')
ip_address = response.json()['ip']
print(f"Railway IP 주소: {ip_address}")
```

또는 Streamlit 앱에 임시로 추가:

```python
import streamlit as st
import requests

# IP 확인 버튼 추가
if st.button("Railway IP 확인"):
    try:
        response = requests.get('https://api.ipify.org?format=json')
        ip_address = response.json()['ip']
        st.success(f"Railway IP 주소: {ip_address}")
        st.info("이 IP 주소를 업비트 API에 등록하세요.")
    except Exception as e:
        st.error(f"IP 확인 실패: {e}")
```

### 방법 2: Railway Logs에서 확인

1. Railway 대시보드 → **"Logs"** 탭
2. 앱이 실행되면 로그에 IP 주소가 표시될 수 있습니다
3. 또는 위의 IP 확인 코드를 실행한 후 로그 확인

### 방법 3: 외부 서비스 사용

Railway 앱이 배포된 후, 다음 URL로 접속하여 IP 확인:

- https://api.ipify.org
- https://ifconfig.me
- https://icanhazip.com

## ⚠️ Railway IP의 특성

1. **동적 IP**: Railway는 컨테이너가 재시작되거나 재배포될 때 IP가 변경될 수 있습니다
2. **여러 IP**: 여러 서비스가 있으면 각각 다른 IP를 가질 수 있습니다
3. **IP 범위**: Railway는 AWS나 다른 클라우드 제공자의 IP 범위를 사용합니다

## 📝 업비트 API IP 등록 방법

### 1. 업비트 웹사이트 접속

1. https://upbit.com 접속
2. 로그인
3. **마이페이지** → **Open API 관리** 이동

### 2. API 키 관리

1. **API 키** 섹션에서 등록된 API 키 확인
2. **IP 주소 등록** 또는 **출입 IP 주소** 섹션 찾기

### 3. IP 주소 추가

1. **"IP 주소 추가"** 또는 **"등록"** 버튼 클릭
2. Railway에서 확인한 IP 주소 입력
   - 예: `123.45.67.89`
3. **저장** 또는 **등록** 클릭

### 4. IP 주소 범위 등록 (선택사항)

업비트가 IP 범위 등록을 지원하는 경우:
- Railway의 IP 범위를 확인하여 등록
- 또는 와일드카드 사용 (예: `123.45.67.*`)

## 🔄 IP 변경 대응 방법

### 방법 1: IP 확인 스크립트 자동화

앱 시작 시 자동으로 IP를 확인하고 로그에 출력:

```python
import requests
import logging

def get_railway_ip():
    """Railway의 현재 IP 주소를 확인합니다."""
    try:
        response = requests.get('https://api.ipify.org?format=json', timeout=5)
        ip = response.json()['ip']
        logging.info(f"Railway 현재 IP 주소: {ip}")
        return ip
    except Exception as e:
        logging.error(f"IP 확인 실패: {e}")
        return None

# 앱 시작 시 실행
if __name__ == "__main__":
    railway_ip = get_railway_ip()
    if railway_ip:
        print(f"⚠️ 업비트 API에 이 IP를 등록하세요: {railway_ip}")
```

### 방법 2: IP 화이트리스트 비활성화 (권장하지 않음)

- 업비트 API 설정에서 IP 제한을 해제할 수 있지만, **보안상 권장하지 않습니다**
- 가능하면 IP 등록을 사용하세요

### 방법 3: 정기적인 IP 확인

- Railway 앱에 IP 확인 기능을 추가
- 주기적으로 IP를 확인하여 변경 시 알림

## 📋 체크리스트

- [ ] Railway 앱 배포 완료
- [ ] Railway IP 주소 확인
- [ ] 업비트 웹사이트에서 API 키 관리 페이지 접속
- [ ] 확인한 IP 주소를 업비트 API에 등록
- [ ] API 호출 테스트 (잔고 조회 등)
- [ ] IP 변경 시 재등록 방법 확인

## 🐛 문제 해결

### "IP 주소가 등록되지 않았습니다" 오류

1. **IP 주소 확인**: Railway 앱에서 현재 IP를 다시 확인
2. **등록 확인**: 업비트 API 관리 페이지에서 IP가 올바르게 등록되었는지 확인
3. **형식 확인**: IP 주소 형식이 올바른지 확인 (예: `123.45.67.89`)

### IP가 자주 변경되는 경우

1. **업비트 IP 범위 등록**: 가능하면 IP 범위로 등록
2. **알림 설정**: IP 변경 시 알림을 받도록 설정
3. **자동 확인**: 앱 시작 시 IP를 확인하고 로그에 출력

## 💡 참고사항

- Railway의 무료 플랜에서는 IP가 더 자주 변경될 수 있습니다
- 유료 플랜을 사용하면 IP가 더 안정적일 수 있습니다
- 업비트 API는 보안을 위해 IP 등록을 권장합니다
