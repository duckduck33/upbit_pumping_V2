# Streamlit 앱 실행 방법

## 📋 사전 준비사항

### 1. Python 설치 확인
- Python 3.8 이상이 설치되어 있어야 합니다.
- 터미널에서 확인: `python --version`

### 2. API 키 파일 준비
- 상위 디렉토리(`업비트3분봉` 폴더)에 `api.json` 파일이 있어야 합니다.
- 파일 내용 예시:
```json
{
    "access_key": "YOUR_ACCESS_KEY",
    "secret_key": "YOUR_SECRET_KEY"
}
```

## 🚀 실행 방법

### 방법 1: 가상환경 사용 (권장)

#### 1단계: 가상환경 생성 및 활성화
```bash
# 프로젝트 루트 디렉토리에서
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

#### 2단계: 패키지 설치
```bash
# streamlit_app 폴더로 이동
cd streamlit_app

# 필요한 패키지 설치
pip install -r requirements.txt
```

#### 3단계: Streamlit 앱 실행
```bash
streamlit run app.py
```

### 방법 2: 전역 환경에서 실행

#### 1단계: streamlit_app 폴더로 이동
```bash
cd streamlit_app
```

#### 2단계: 패키지 설치
```bash
pip install streamlit pandas pyupbit requests rich
```

#### 3단계: Streamlit 앱 실행
```bash
streamlit run app.py
```

## 🌐 접속

실행 후 자동으로 브라우저가 열립니다:
- 기본 주소: `http://localhost:8501`
- 브라우저가 자동으로 열리지 않으면 위 주소를 직접 입력하세요.

## 📖 사용 방법

### 1. 사이드바에서 설정
- **분봉 선택**: 1분봉, 2분봉, 3분봉 등 선택
- **기준 시간**: 분석할 시간 설정 (시/분)
- **필터링 조건**:
  - 가격 변동률 최소/최대 (%)
  - 거래량 변동 최소 (%)
  - 슬리피지 최대 (%)
- **일봉 필터링**: 체크 시 활성화
- **제외 코인**: 콤마로 구분 (예: `BTC,ETH,ONDO`)
- **자동매매**: 체크 시 자동매매 옵션 표시

### 2. 분석 시작
- "🚀 분석 시작" 버튼 클릭
- 진행 상황과 로그가 실시간으로 표시됩니다

### 3. 결과 확인
- 필터링 결과가 테이블로 표시됩니다
- "📥 CSV 다운로드" 버튼으로 결과 저장

### 4. 자동매매 실행 (선택사항)
- 자동매매가 활성화된 경우 "매수 실행" 버튼이 나타납니다
- 버튼을 클릭하면 자동으로 매수 주문이 실행됩니다

## ⚠️ 주의사항

1. **API 키 보안**
   - `api.json` 파일은 절대 공개하지 마세요
   - Git에 커밋하지 않도록 `.gitignore`에 추가하세요

2. **상위 디렉토리 파일 필요**
   - `auto_trading_system_gui.py` 파일이 상위 디렉토리에 있어야 합니다
   - 이 파일의 함수들을 재사용합니다

3. **인터넷 연결**
   - 업비트 API 호출을 위해 인터넷 연결이 필요합니다

## 🔧 문제 해결

### Import 오류 발생 시
```
ModuleNotFoundError: No module named 'auto_trading_system_gui'
```
**해결**: 상위 디렉토리에 `auto_trading_system_gui.py` 파일이 있는지 확인하세요.

### API 키 오류 발생 시
```
API 키를 불러올 수 없습니다.
```
**해결**: 상위 디렉토리에 `api.json` 파일이 올바르게 설정되어 있는지 확인하세요.

### 포트가 이미 사용 중일 때
```
Port 8501 is already in use
```
**해결**: 
- 다른 Streamlit 앱을 종료하거나
- 다른 포트 사용: `streamlit run app.py --server.port 8502`

### 패키지 설치 오류
```
ERROR: Could not find a version that satisfies the requirement
```
**해결**: 
- Python 버전 확인 (3.8 이상 필요)
- pip 업그레이드: `python -m pip install --upgrade pip`

## 📝 빠른 시작 명령어

```bash
# 1. streamlit_app 폴더로 이동
cd streamlit_app

# 2. 패키지 설치 (처음 한 번만)
pip install -r requirements.txt

# 3. 앱 실행
streamlit run app.py
```

## 🎯 실행 확인

정상적으로 실행되면:
1. 터미널에 "You can now view your Streamlit app in your browser." 메시지가 표시됩니다
2. 브라우저가 자동으로 열리고 웹 인터페이스가 표시됩니다
3. 사이드바에 설정 옵션들이 보입니다
